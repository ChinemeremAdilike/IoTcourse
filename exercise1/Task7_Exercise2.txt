#include <Servo.h>

Servo myservo;

void setup() {
  myservo.attach(5);
}

void loop() {
  int potValue = analogRead(A0);
  int angle = map(potValue, 0, 1023, 0, 180);
  myservo.write(angle);
  delay(10);
}